# arrvl

the arrvl.studio site. design, development, and motion.

built with next.js (app router), react 19, tailwind v4, and gsap. the contact form posts to a separate crm.

## local dev

```
npm install
npm run dev     # http://localhost:3000
npm run build   # production build
```
